export type ManualItem = {
  title: string;
  subtitle: string;
  description: string;
  href: string;
  size?: string;
};

export type VideoItem = {
  id: string;
  title: string;
  description: string;
  duration?: string;
};